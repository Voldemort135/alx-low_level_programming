#include <stdio.h>

/**
 * main - prints a phrase
 * Return: Always 0
 */

int main(void)
{
	char phrase[] = "with proper grammar, but the outcome is a piece of art,";
		printf("%s\n", phrase);
		return (0);
}
